# Flag that toggles whether charging can happen
bool enable_charge

# Flag that indicates whether battery should instantaneously charge to 100%
bool enable_instant_charge

# Flag that toggles whether battery drain should occur
bool enable_drain